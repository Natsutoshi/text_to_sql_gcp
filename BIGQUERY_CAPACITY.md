# BigQuery無料枠に収まる最大レコード数の計算結果

## BigQuery無料枠の制限

- **ストレージ**: 10GB/月（無料）
- **クエリ処理**: 1TB/月（無料）

## 現在のデータサイズ（参考）

| テーブル | レコード数 | ファイルサイズ | 1レコードあたり |
|---------|----------|--------------|----------------|
| customers | 20 | 2.66KB | 136.2バイト |
| products | 20 | 3.18KB | 162.7バイト |
| orders | 20 | 1.67KB | 85.2バイト |
| order_items | 40 | 0.94KB | 24.1バイト |
| **合計** | **100** | **8.44KB** | - |

## 理論上の最大レコード数（単独テーブル）

各テーブルを単独で10GBに収める場合：

| テーブル | 最大レコード数 |
|---------|--------------|
| customers | 約236,600,000（約2.4億） |
| products | 約198,000,000（約2.0億） |
| orders | 約377,900,000（約3.8億） |
| order_items | 約1,336,600,000（約13.4億） |

## リレーションシップを考慮した推奨値

Eコマースのリレーションシップを考慮し、ストレージを以下のように配分：

- **customers**: 20%（基準テーブル）
- **products**: 20%（商品マスタ）
- **orders**: 30%（1顧客あたり複数注文）
- **order_items**: 30%（1注文あたり複数商品）

### 安全な推奨値（無料枠の80%使用）

| テーブル | 推奨レコード数 | 推定ストレージ（圧縮後） |
|---------|--------------|----------------------|
| customers | **約3,800万** | 約4.8GB |
| products | **約3,200万** | 約4.8GB |
| orders | **約9,100万** | 約7.2GB |
| order_items | **約3億2,000万** | 約7.2GB |
| **合計** | - | **約24GB（圧縮前）→ 約8GB（圧縮後）** |

### 実用的な推奨値（学習・テスト用途）

| テーブル | 推奨レコード数 | 推定ストレージ（圧縮後） |
|---------|--------------|----------------------|
| customers | **1,000万** | 約1.3GB |
| products | **100万** | 約0.2GB |
| orders | **5,000万** | 約1.3GB |
| order_items | **1億5,000万** | 約1.1GB |
| **合計** | - | **約3.9GB（圧縮前）→ 約1.3GB（圧縮後）** |

✓ **無料枠内に十分収まります！**

## データ生成の推奨設定

### 小規模（テスト用）
- customers: 10,000件
- products: 1,000件
- orders: 50,000件
- order_items: 150,000件
- **推定ストレージ**: 約1.3MB（圧縮後）

### 中規模（開発用）
- customers: 100,000件
- products: 10,000件
- orders: 500,000件
- order_items: 1,500,000件
- **推定ストレージ**: 約13MB（圧縮後）

### 大規模（本番テスト用）
- customers: 1,000,000件（100万）
- products: 100,000件（10万）
- orders: 5,000,000件（500万）
- order_items: 15,000,000件（1,500万）
- **推定ストレージ**: 約130MB（圧縮後）

### 最大規模（無料枠上限）
- customers: 10,000,000件（1,000万）
- products: 1,000,000件（100万）
- orders: 50,000,000件（5,000万）
- order_items: 150,000,000件（1億5,000万）
- **推定ストレージ**: 約1.3GB（圧縮後）

## 注意事項

1. **圧縮率**: BigQueryはデータを約5-10倍圧縮しますが、安全のため3倍として計算しています
2. **インデックス**: 上記の計算にはインデックスのサイズは含まれていません
3. **メタデータ**: テーブルスキーマやメタデータのサイズは含まれていません
4. **クエリ処理**: ストレージとは別に、クエリ処理量（1TB/月）にも注意が必要です

## データ生成方法

`generate_large_dataset.py`スクリプトを使用して、指定したレコード数のデータを生成できます。

```bash
# 中規模データセットを生成
python3 generate_large_dataset.py --customers 100000 --products 10000 --orders 500000

# 大規模データセットを生成
python3 generate_large_dataset.py --customers 1000000 --products 100000 --orders 5000000
```

